# game_inuyasha2d

Juego 2D de Inuyasha en HTML.

- Juego: `public/index.html` (abrir ese archivo para jugar localmente).
- Assets: `public/assets/`.
- Cloudflare: Workers Static Assets, config en `wrangler.jsonc` (sirve `./public`). `npm install` y luego `npm run dev` o `npm run deploy`.
